{
	"name": "@scripts/admin",
	"version": "0.0.0",
	"private": true,
	"license": "MIT",
	"scripts": {
		"format": "bunx biome format --write .",
		"lint": "bunx biome lint --write ./src",
		"dev:generate-admin": "bun run ./src/generateAdmin.ts",
		"dev:init-minio": "bun run ./src/initMinio.ts"
	},
	"dependencies": {
		"find-up": "^7.0.0",
		"minio": "^8.0.3"
	}
}
