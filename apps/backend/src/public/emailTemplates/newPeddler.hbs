<head>
  <title>New Peddler</title>
  <style>
    .container {
      font-family: Arial, Helvetica, sans-serif;
    }
    .title {
      font-size: 20px;
      font-weight: bold;
    }
    .text {
      font-size: 16px;
    }
    .table {
      border: 1px solid black;
      border-collapse: collapse;
    }
    .table td {
      border: 1px solid black;
      padding: 5px;
    }
    .bold {
      font-weight: bold;
    }
    .link {
      color: #a855f7;
      border: none;
      text-align: center;
      text-decoration: none;
      display: inline-block;
    }
  </style>
</head>
<body class="container">
  <div>
    <h1 class="title">New TSP peddler</h1>
    <p class="text">A new peddler was observed at the following location.</p>
    <table class="table">
      <tbody>
        {{#each attributes}}
          <tr>
            <td class="bold">{{this.key}}</td>
            <td>{{this.value}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
    <p class="text">
      View all the details of this peddler here:
      <span>
        <a href="{{viewFullUrl}}" class="link">{{viewFullUrl}}</a>
      </span>
    </p>
  </div>
</body>
